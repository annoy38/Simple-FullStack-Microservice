server:
  port: 8086

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/annoy38/blood-and-eye-donation-bank-configuration-server
          clone-on-start: true

